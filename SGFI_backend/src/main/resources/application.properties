# SGFI — CHU Hassan II Fès — Configuration Spring Boot 3.x

# --- Base de données MySQL ---
#spring.datasource.url=jdbc:mysql://localhost:3306/sgfi1_bd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/sgfi1_bd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# ↓ SUPPRIMÉ : Hibernate le détecte tout seul avec Spring Boot 3.x — inutile + génère un warning
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ↓ AJOUTÉ : désactive le mode "open-in-view" (inutile pour une API REST stateless)
spring.jpa.open-in-view=false

# --- JWT ---
app.jwt.secret=SGFISecretKey2024CHUHassanIIFesMarocJWT!
app.jwt.expiration=86400000

# --- Multipart (import Excel CCR) ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# --- Serveur ---
server.port=8080

# --- Stockage des pièces justificatives ---
file.upload-dir=C:/sgfi-documents
file.max-file-size=10485760